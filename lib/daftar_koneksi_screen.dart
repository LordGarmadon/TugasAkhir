import 'package:flutter/material.dart';

class DaftarKoneksi extends StatelessWidget {
  const DaftarKoneksi({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Daftar Koneksi");
  }
}
